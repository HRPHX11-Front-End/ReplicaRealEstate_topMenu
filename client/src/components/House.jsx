import React from 'react';

const House = ({houseData}) => {
  // eslint-disable-next-line react/destructuring-assignment
  // eslint-disable-next-line react/prop-types
  console.log(houseData.color);
  return (
    <div>Ollo?</div>
  );
};

export default House;
